package escobar.testepratico;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class TestePraticoApplication {

	/**
	 * Método principal para executar a aplicação.
	 * @param args array de Strings -  Array de Strings que possibilita passagem de parâmetros para aplicação.
	 */
	public static void main(String[] args) {
		SpringApplication.run(TestePraticoApplication.class, args);
	}

}
